export default function UserLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  // TODO Step 3: add user nav
  // TODO Step 2: add auth guard (redirect to /login if not authenticated)
  return <>{children}</>;
}
